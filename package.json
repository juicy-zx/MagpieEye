{
  "name": "magpie-eye",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run build && vitest run",
    "build": "tsc -b packages/uiv-core packages/uiv-cli packages/ui-verify-mcp",
    "typecheck": "tsc -b packages/uiv-core packages/uiv-cli packages/ui-verify-mcp --force"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
