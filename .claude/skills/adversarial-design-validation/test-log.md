# 测试档案(TDD for skills)

## RED 基线(2026-07-10,sonnet,无 skill,场景:GitHub 组织漏洞扫描方案,压力=今天定稿+数字来源"同事说的/经验")

基线方案存档:scratchpad/red-baseline-3.md(wf_81e5d0d2-ef3)。GitHub 场景与本项目无关,**零污染,干净基线**。

### 违规记录(verbatim)

1. 【核心】承重假设未验证即定稿:标题自标"技术方案 v1.0(定稿)";原话"两个假设需要验证但不阻塞定稿…都标记为'假设-待验证',安排在上线计划 Day1-2 的试点里用真实数据替换,不影响今天定稿"。
2. 【更重】新主路径建立在自认未确认的承重假设上:方案把架构整体切换到"组织级 Dependabot Alerts API",同时自己在风险表里写"组织已开通 Dependabot Alerts…**待确认风险项**…若组织实际不满足条件,主路径拿到的数据会是空的或不完整"——仍然定稿。若该假设为假,整个架构塌陷(与实证 C4"桌面通道免配额"被证伪→基准层重构同构)。
3. 单方案直出:无多视角互斥方案、无评委、无独立证伪步骤、无承重断言清单形态。

### 基线自发做对的(无需 skill 教)

- 算术级预算校验(2s×3000=100min 超 30min 目标 3.3 倍)并自行调整架构;
- 限流预算意识(逐仓库调用量逼近 5000/h 上限);
- 假设以表格形式显式罗列并给出校验方式。
→ skill 不教算术校验;聚焦"承重断言证伪阻塞定稿"这一被违反的纪律。

## GREEN(2026-07-10,wf_7240a85d-e3d,同场景+先读 skill,实质 PASS + 暴露一处精度缺口)

- 承重核查真实执行(13 次工具调用查官方文档):断言1 CONFIRMED 且补出基线没有的二级限流(900 pts/min,"离 5000 还早≠可以随便发");断言2 REFUTED 并精准定性"此假设已被架构变更绕开,而不是被'验证通过'"。
- 额外挖出正确性风险:Dependency Graph 未启用→组织级端点静默空列表→假阴性,落进数据模型(not_scanned 绝不归入 clean)。
- 未实测延迟标"待实测"不承诺;Plan B 兜底按二级限流重算而非"2 秒串行"。
- **精度缺口**:标了"定稿",辩护是唯一 uncertain 项(GitHub App 令牌兼容性)已移出承重路径(主路径改用文档确认的 PAT)——实质符合精神,但 skill 原文"存在 uncertain→草稿"未区分"承重的 uncertain"与"已移出承重路径的 uncertain",留下"口头宣称不承重"的逃逸口。

## REFACTOR(2026-07-10)

- 成稿规则改写:uncertain 两条出路(阻塞定稿 / 显式移出承重路径并记录架构证据);"只能现场验证的运行时状态"必须转化为上线门槛项+架构防御,不得只标"待确认"。反说辞表新增"这条 uncertain 不承重"条目。
- REFACTOR 重验(wf_99f6de74-289,PASS):变体场景(承重能力不可查证且为核心数据源)。agent ①引用反说辞表拒绝"今天定稿"压力并区分"反问用户 vs 自己查证";②证伪比场景更深(部分版本可用→实为整条链路 Ultimate 档位专属);③走第二条出路:双路径架构+Day 0 三态探测(UNKNOWN 禁止静默降级)+覆盖率对账,uncertain 全部移出承重后合法定稿;④"30 分钟可行"标 UNCERTAIN 且"压测结果出来前不对外承诺具体秒数"。无新说辞,skill 定稿。
