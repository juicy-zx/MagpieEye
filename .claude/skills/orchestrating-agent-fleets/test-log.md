# 测试档案(TDD for skills)

## RED 基线(2026-07-10,sonnet,无 skill,场景:8 任务共享工作树编排,压力=赶进度+token 紧张)

基线方案存档:scratchpad/red-baseline-1.md(wf_81e5d0d2-ef3)。
注:基线 agent 跑在本项目目录,受项目文档污染(引用了 meta.json/android-kotlin-delegation 3 处)——污染只会让基线偏好,以下违规是"有项目纪律加持仍犯"的,证据效力更强。

### 违规记录(verbatim)

1. **停机等通知(核心违规,本项目史上复现 4 次的同款坑)**:T3 等待方案原话——"必须用后台执行+等通知的方式""发起后立即让出回合,不要用短 sleep 连续轮询——那样每次轮询都是一次工具调用,纯粹烧 token""收到'后台任务完成'的系统通知后,再去读日志文件尾部"。
   → 说辞"轮询烧 token"已收入反说辞表。
2. **共享文件无登记制**:T7 方案用"时序隔离+基线 commit 校验+小步提交",无"任务本体不碰共享文件、登记清单、单一收尾 agent 串行集成"概念。
3. **无时间止损**:只有"≤2 次尝试停下汇报"的逻辑止损,缺 30 分钟时间止损。

### 基线做对的(无需 skill 教,但收录为参考)

- 模型分层(场景给了档位选项,提示性强,不算干净证据)
- git 精确路径 add / 禁 -A(疑似项目污染所致)
- 验收 agent 独立于实现 agent

## GREEN(2026-07-10,wf_4e6842e4-931,同场景+先读 skill,PASS)

- T3 等待:修复——agent 原话"起了后台任务就说'请稍后查看'然后结束回合——这是明确的悬死场景";改为同回合内前台短轮询循环(sleep 60+tail+ps),终态判定看日志内容+进程退出双证据。
- 五条款逐字进全部 8 个派发词;T7 升级为登记制+单一收尾 agent+逻辑锁("结构性消灭冲突,不依赖时序运气");双重止损落地;模型分层保持并逐条对照分层表。
- 迁移性应用:自发将 T8(快照测试)识别为写偏任务并排他——条款 5 在场景未显式提示下被正确应用。
- 验收把关:主会话亲自 git show 复核收尾 commit,不轻信自报。

## REFACTOR(2026-07-10)

GREEN 一轮无新说辞、无绕行,反说辞表首轮即命中(基线原话"轮询烧 token"被正面驳回)。无需加条款,skill 定稿。
